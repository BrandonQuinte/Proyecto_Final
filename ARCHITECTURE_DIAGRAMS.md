# 📐 Diagramas de Arquitectura - Proyecto Final

Documento complementario con diagramas detallados de la arquitectura del sistema.

---

## 1. Diagrama C4 Modelo - Nivel 1 (Contexto)

```
┌───────────────────────────────────────────────────────────────────────────┐
│                          SISTEMA TODO LIST                               │
│                        (Fullstack Application)                           │
│                                                                           │
│  Desarrollado con:                                                       │
│  • Frontend: React 18.2 + Vite 5                                        │
│  • Backend: Node.js 20 + Express 4.18                                   │
│  • Database: MySQL                                                       │
│  • Deployment: Vercel (FE), Render (BE + DB)                           │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
              △              △              △              △
              │              │              │              │
    ┌─────────┴────┐  ┌──────┴─────┐  ┌────┴──────┐  ┌─────┴──────┐
    │              │  │            │  │           │  │            │
┌───▼──────┐  ┌────▼──────┐  ┌────▼──┐  ┌───▼───────┐
│   USER   │  │ DEVELOPER │  │  VCS  │  │ MONITORING│
│          │  │           │  │(GitHub)  │            │
│• Browser │  │• Local Dev│  │         │ • Vercel  │
│• Mobile  │  │• IDE      │  │• Git   │ • Render  │
│• Desktop │  │• CLI      │  │• Actions│ • Logs    │
└───┬──────┘  └────┬──────┘  └────┬──┘  └───┬───────┘
    │              │               │         │
    └──────────────┴───────────────┴─────────┘
           Interacción
```

---

## 2. Diagrama C4 Modelo - Nivel 2 (Contenedores)

```
USUARIO/BROWSER
    │
    │ HTTPS
    │
    ├─────────────────────────────────────────┐
    │                                         │
    ▼                                         ▼
┌─────────────────────┐          ┌──────────────────────┐
│   VERCEL CDN        │          │  GITHUB PAGES        │
│   (Frontend)        │          │  (Backup)            │
│                     │          │                      │
│ • React App         │          │ • Static files       │
│ • Vite bundled      │          │ • gh-pages branch    │
│ • Global CDN        │          │                      │
│ • HTTPS             │          │ URL:                 │
│ • Automatic deploy  │          │ brandonquinte.       │
│                     │          │ github.io/...        │
│ URL:                │          └──────────────────────┘
│ proyecto-final-     │
│ 9z34.vercel.app     │
└────────┬────────────┘
         │
         │ API REST
         │ VITE_API_URL
         │
         ▼
┌───────────────────────────────────────────┐
│      RENDER BACKEND                       │
│      (Node.js Express Server)             │
│                                           │
│  ┌────────────────────────────────────┐  │
│  │ Express.js API Server              │  │
│  │ • CORS enabled                     │  │
│  │ • Routes: /api/todos               │  │
│  │ • Port: 4000                       │  │
│  │ • Node.js 20                       │  │
│  └────────────────────────────────────┘  │
│           │                               │
│           │ Sequelize ORM                │
│           │ MySQL Connection             │
│           ▼                               │
│  ┌────────────────────────────────────┐  │
│  │ MySQL Database (Render)            │  │
│  │ • Database: todos_db               │  │
│  │ • Table: todos                     │  │
│  │ • Backups: automatic               │  │
│  │ • SSL/TLS encryption               │  │
│  └────────────────────────────────────┘  │
│                                           │
│ URL:                                      │
│ proyecto-final-rqns.onrender.com         │
└───────────────────────────────────────────┘
```

---

## 3. Componentes del Frontend (Árbol de Componentes)

```
src/
├── main.jsx (Entry point)
│   └── App.jsx (Root component)
│       │
│       └── Home.jsx (Main page)
│           │
│           ├── TodoList.jsx
│           │   │
│           │   ├── TodoItem.jsx
│           │   │   ├─ Props: todo, onEdit, onDelete, onToggle
│           │   │   ├─ State: Local UI state
│           │   │   └─ Actions: Edit, Delete, Toggle status
│           │   │
│           │   └── Empty state message
│           │       └─ "No hay tareas"
│           │
│           └── TodoForm.jsx
│               ├─ Props: onSubmit, initialValues
│               ├─ State: title, description, isEditing
│               ├─ Input fields
│               │   ├─ title (required)
│               │   └─ description (optional)
│               └─ Submit button
│                   ├─ "+ Nueva tarea" (create mode)
│                   └─ "✎ Editar tarea" (edit mode)

services/
├── api.js (Axios HTTP client)
│   ├─ GET /api/todos → getTodos()
│   ├─ POST /api/todos → createTodo()
│   ├─ PUT /api/todos/:id → updateTodo()
│   └─ DELETE /api/todos/:id → deleteTodo()

styles/
├── index.css (Global: typography, colors, layout)
├── home.css (Page layout, header styling)
├── form.css (Form inputs, submit button)
├── list.css (List container, empty state)
└── item.css (Todo card, action buttons)
```

---

## 4. Componentes del Backend (Arquitectura por Capas)

```
src/
├── index.js (Entry point - Express server)
│   │
│   ├─ CORS Configuration
│   ├─ Body Parser Middleware
│   ├─ Error Handler Middleware
│   └─ Route Registration
│
├── config/
│   └── db.js (Database Connection)
│       ├─ Sequelize instance
│       ├─ MySQL connection pool
│       └─ Model associations
│
├── models/
│   └── todoModel.js (Data Model)
│       ├─ Attributes:
│       │   ├─ id (PK, Auto-increment)
│       │   ├─ title (String, Required)
│       │   ├─ description (Text, Optional)
│       │   ├─ status (Enum: pending|completed)
│       │   ├─ createdAt (Timestamp)
│       │   └─ updatedAt (Timestamp)
│       │
│       └─ Methods:
│           ├─ findAll()
│           ├─ create()
│           ├─ update()
│           └─ destroy()
│
├── routes/
│   └── todos.js (API Routes)
│       ├─ GET /api/todos
│       ├─ POST /api/todos
│       ├─ PUT /api/todos/:id
│       └─ DELETE /api/todos/:id
│
└── controllers/
    └── todosController.js (Business Logic)
        ├─ getTodos()
        │   └─ Query DB & return array
        │
        ├─ createTodo()
        │   ├─ Validate input (title required)
        │   ├─ Sanitize data
        │   ├─ Insert into DB
        │   └─ Return created todo (201)
        │
        ├─ updateTodo()
        │   ├─ Find by ID
        │   ├─ Validate update fields
        │   ├─ Update in DB
        │   └─ Return updated todo (200)
        │
        └─ deleteTodo()
            ├─ Find by ID
            ├─ Delete from DB
            └─ Return success message (200)
```

---

## 5. Flujo de Datos - Crear Tarea (Detallado)

```
USUARIO INTERACTION:
┌─────────────────┐
│ Usuario escribe │ → Input: "Comprar leche"
│ en formulario   │
└────────┬────────┘
         │ onChange event
         ▼
┌─────────────────────────────────────────┐
│ TodoForm.jsx                            │
│                                         │
│ const [title, setTitle] = useState('')  │
│ setTitle('Comprar leche')              │
└────────┬────────────────────────────────┘
         │ Validación local
         │ if (!title.trim()) return;
         │
         │ Usuario cliquea botón "+"
         │
         ▼
┌─────────────────────────────────────────┐
│ handleSubmit() ejecuta                  │
│                                         │
│ 1. Prevenir form refresh               │
│ 2. Validar título no vacío             │
│ 3. Preparar payload JSON               │
└────────┬────────────────────────────────┘
         │
         │ Axios HTTP POST
         │ {
         │   "title": "Comprar leche",
         │   "description": ""
         │ }
         │
         ▼
    ┌────────────────────────────────────────────────────────┐
    │ NETWORK TRANSMISSION (100-200ms)                       │
    │                                                        │
    │ POST https://proyecto-final-rqns.onrender.com/api/todos
    │ Headers: Content-Type: application/json               │
    │ Body: JSON payload                                     │
    └────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────┐
│ BACKEND - todos.js (Route Handler)                  │
│                                                      │
│ router.post('/', todoController.createTodo)         │
│                                                      │
│ Step 1: Parse request body                          │
│ Step 2: Extract { title, description }              │
│ Step 3: Pass to controller                          │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ BACKEND - todosController.js                        │
│                                                      │
│ exports.createTodo = async (req, res) => {          │
│   const { title, description } = req.body;          │
│                                                      │
│   // Validación backend                             │
│   if (!title || title.trim() === '') {             │
│     return res.status(400).json({                   │
│       error: 'El título es obligatorio'             │
│     });                                              │
│   }                                                  │
│                                                      │
│   // Crear en BD                                     │
│   const todo = await Todo.create({                  │
│     title: title.trim(),                            │
│     description: description || null,               │
│     status: 'pending'                               │
│   });                                                │
│                                                      │
│   // Responder                                       │
│   res.status(201).json(todo);                       │
│ }                                                    │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ DATABASE - Sequelize & MySQL                        │
│                                                      │
│ SQL Generated by Sequelize:                         │
│                                                      │
│ INSERT INTO todos                                    │
│   (title, description, status, created_at, updated_at)
│ VALUES                                               │
│   ('Comprar leche', NULL, 'pending', NOW(), NOW()) │
│                                                      │
│ MySQL Response:                                      │
│ - Inserts row                                        │
│ - Generates ID: 42 (AUTO_INCREMENT)                 │
│ - Returns 1 (row affected)                          │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ BACKEND - Return Response                           │
│                                                      │
│ HTTP/1.1 201 Created                                │
│ Content-Type: application/json                      │
│                                                      │
│ {                                                    │
│   "id": 42,                                          │
│   "title": "Comprar leche",                          │
│   "description": null,                               │
│   "status": "pending",                               │
│   "createdAt": "2025-12-10T12:00:00Z",             │
│   "updatedAt": "2025-12-10T12:00:00Z"              │
│ }                                                    │
└──────────────────────┬───────────────────────────────┘
                       │
    ┌──────────────────┴──────────────────┐
    │ NETWORK TRANSMISSION (100-200ms)    │
    └──────────────────┬──────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ FRONTEND - Axios Response Handler                   │
│                                                      │
│ API.interceptors.response.use(                      │
│   (response) => {                                    │
│     return response.data;  // ← Retorna data        │
│   }                                                  │
│ )                                                    │
│                                                      │
│ const response = await createTodo(newTodo);         │
│ // response = { id: 42, title: ..., status: ... }  │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ FRONTEND - Home.jsx (State Update)                  │
│                                                      │
│ const [todos, setTodos] = useState([]);             │
│                                                      │
│ const handleAddTodo = async (newTodo) => {          │
│   try {                                              │
│     const response = await createTodo(newTodo);     │
│     // Actualizar estado                            │
│     setTodos([...todos, response]);                 │
│     // Limpiar formulario                           │
│     setFormData({ title: '', description: '' });   │
│   } catch (error) {                                 │
│     console.error('Error:', error);                 │
│   }                                                  │
│ }                                                    │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ REACT RECONCILIATION & RE-RENDER                    │
│                                                      │
│ 1. setTodos() dispara setState                      │
│ 2. React compara old vs new virtual DOM             │
│ 3. Detecta nuevo item en lista                      │
│ 4. Agrega <TodoItem> al DOM                         │
│ 5. CSS styles se aplican automáticamente            │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│ ✅ TAREA VISIBLE EN LA UI                           │
│                                                      │
│ Nueva tarea renderizada:                            │
│                                                      │
│ ┌─────────────────────────────────┐                │
│ │ ◐ Comprar leche                 │ ← status     │
│ │                                 │                │
│ │ [✎ Editar] [✓ Completar] [✕ Eliminar]         │
│ └─────────────────────────────────┘                │
│                                                      │
│ ✅ Todo el ciclo completado                        │
└──────────────────────────────────────────────────────┘
```

---

## 6. Modelos de Base de Datos

### Tabla: todos

```
Estructura Física:
┌─────┬──────────────┬──────────────┬─────────┬──────────────┬──────────────┐
│ ID  │ TITLE        │ DESCRIPTION  │ STATUS  │ CREATED_AT   │ UPDATED_AT   │
├─────┼──────────────┼──────────────┼─────────┼──────────────┼──────────────┤
│ 1   │ Comprar pan  │ Pan tajado   │ pending │ 2025-12-10   │ 2025-12-10   │
│     │              │              │         │ 08:30:00     │ 08:30:00     │
├─────┼──────────────┼──────────────┼─────────┼──────────────┼──────────────┤
│ 2   │ Hacer        │ 30 minutos   │ complete│ 2025-12-09   │ 2025-12-09   │
│     │ ejercicio    │ de cardio    │ d       │ 07:15:00     │ 18:45:00     │
├─────┼──────────────┼──────────────┼─────────┼──────────────┼──────────────┤
│ 3   │ Estudiar SQL │ NULL         │ pending │ 2025-12-10   │ 2025-12-10   │
│     │              │              │         │ 10:00:00     │ 10:00:00     │
└─────┴──────────────┴──────────────┴─────────┴──────────────┴──────────────┘
```

### Índices

```
PRIMARY KEY (id)
  └─ Identifica única cada fila
  └─ Automáticamente AUTO_INCREMENT
  
INDEX (created_at)
  └─ Optimiza búsquedas por fecha
  
INDEX (status)
  └─ Optimiza filtros por estado (pending/completed)
```

---

## 7. Flujo de CI/CD Visual

```
┌─────────────────┐
│  git push       │
│  master branch  │
└────────┬────────┘
         │
         ▼
┌──────────────────────────────────┐
│  GitHub Webhook Triggered        │
│  (.github/workflows/ci.yml)      │
└────────┬─────────────────────────┘
         │
         ├─────────────────┬────────────────────┬──────────────────┐
         │                 │                    │                  │
         ▼                 ▼                    ▼                  ▼
    ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  ┌─────────────┐
    │ Checkout    │  │ Setup Node   │  │ Install FE     │  │ Build Vite  │
    │ Repository  │  │ v20          │  │ Dependencies   │  │ (dist/)     │
    └─────────────┘  └──────────────┘  └────────────────┘  └──────┬──────┘
                                                                   │
                                                                   ▼
                                                            ┌──────────────────┐
                                                            │ Deploy to        │
                                                            │ GitHub Pages     │
                                                            │ (gh-pages branch)│
                                                            └────────┬─────────┘
                                                                     │
                ┌────────────────────────────────────────┬──────────┤
                │                                        │          │
                ▼                                        ▼          ▼
         ┌──────────────┐                        ┌──────────────────┐
         │ Vercel Auto  │                        │ Render Auto      │
         │ Detects Push │                        │ Detects Push     │
         └──────┬───────┘                        └────────┬─────────┘
                │                                        │
                ├─ npm install                           ├─ npm install
                ├─ npm run build                         ├─ npm start
                ├─ Deploy to CDN                         └─ Health Check
                │                                        
                ▼                                        ▼
         ┌──────────────────────┐            ┌──────────────────────┐
         │ Vercel Deployment    │            │ Render Deployment    │
         │ ✅ LIVE (2-3 min)    │            │ ✅ LIVE (3-4 min)    │
         │                      │            │                      │
         │ proyecto-final-      │            │ proyecto-final-      │
         │ 9z34.vercel.app      │            │ rqns.onrender.com    │
         └──────────────────────┘            └──────────────────────┘

Resultado:
✅ GitHub Pages LIVE (brandonquinte.github.io/Proyecto_Final/)
✅ Vercel LIVE (proyecto-final-9z34.vercel.app)
✅ Render LIVE (proyecto-final-rqns.onrender.com/api)
```

---

## 8. Matriz de Tecnologías por Capa

```
┌─────────────────────────────────────────────────────────────────┐
│ CAPA                │ TECNOLOGÍA        │ VERSION │ PROPÓSITO    │
├─────────────────────────────────────────────────────────────────┤
│ PRESENTACIÓN (UI)   │ React             │ 18.2    │ Components   │
│                     │ CSS 3             │ -       │ Styling      │
│                     │ Vite              │ 5.0     │ Build Tool   │
├─────────────────────────────────────────────────────────────────┤
│ CONSUMO API         │ Axios             │ 1.6+    │ HTTP Client  │
├─────────────────────────────────────────────────────────────────┤
│ BACKEND             │ Node.js           │ 20.x    │ Runtime      │
│ (Server)            │ Express.js        │ 4.18    │ Web Server   │
├─────────────────────────────────────────────────────────────────┤
│ MAPEO DATOS (ORM)   │ Sequelize         │ 6.37    │ ORM Mapper   │
├─────────────────────────────────────────────────────────────────┤
│ BASE DATOS          │ MySQL             │ 8.0+    │ RDBMS        │
│                     │ MySQL2            │ 3.15    │ Driver       │
├─────────────────────────────────────────────────────────────────┤
│ DEPLOYMENT FE       │ Vercel            │ -       │ CDN Hosting  │
│ DEPLOYMENT BE       │ Render            │ -       │ App Hosting  │
│ DEPLOYMENT DB       │ Render MySQL      │ -       │ DB Hosting   │
├─────────────────────────────────────────────────────────────────┤
│ CI/CD               │ GitHub Actions    │ v4      │ Automation   │
│                     │ Git               │ 2.x+    │ VCS          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. Matriz de Seguridad por Componente

```
┌────────────────────────────────────────────────────────────┐
│ COMPONENTE          │ MEDIDAS DE SEGURIDAD                 │
├────────────────────────────────────────────────────────────┤
│ Frontend (React)    │ • HTTPS obligatorio (Vercel)        │
│                     │ • CORS prevención de XSS             │
│                     │ • Validación local de inputs         │
│                     │ • No almacenar datos sensibles       │
├────────────────────────────────────────────────────────────┤
│ API (Express)       │ • CORS whitelist (solo Vercel)      │
│                     │ • Validación de entrada              │
│                     │ • SQL Injection prevention (ORM)    │
│                     │ • Error handling sin data leaks      │
│                     │ • HTTPS en producción (Render)      │
├────────────────────────────────────────────────────────────┤
│ Base de Datos       │ • Conexión SSL/TLS                   │
│                     │ • Variables de entorno protegidas    │
│                     │ • No exponer DATABASE_URL            │
│                     │ • Backups automáticos (Render)       │
│                     │ • Read-only access cuando sea posible│
├────────────────────────────────────────────────────────────┤
│ Deployment          │ • GitHub secret tokens               │
│                     │ • Environment variables encriptadas  │
│                     │ • Logs auditados                     │
│                     │ • Rate limiting recomendado          │
└────────────────────────────────────────────────────────────┘
```

---

## 10. Tabla Comparativa: Desarrollo vs Producción

```
┌──────────────────┬──────────────────────┬──────────────────────┐
│ ASPECTO          │ DESARROLLO LOCAL     │ PRODUCCIÓN           │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Frontend URL     │ http://localhost:5173│ https://proyecto...  │
│                  │                      │ final-9z34.vercel.app│
├──────────────────┼──────────────────────┼──────────────────────┤
│ Backend URL      │ http://localhost:4000│ https://proyecto...  │
│                  │                      │ final-rqns.onrender  │
│                  │                      │ .com                 │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Database         │ Local MySQL          │ Render MySQL         │
│                  │ (MySQL server local) │ (Cloud managed)      │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Build Tool       │ Vite dev server (HMR)│ Vite production build│
├──────────────────┼──────────────────────┼──────────────────────┤
│ Deploy          │ Manual (npm run dev) │ Automático (Git push)│
├──────────────────┼──────────────────────┼──────────────────────┤
│ Logs            │ Console local        │ Vercel/Render logs   │
├──────────────────┼──────────────────────┼──────────────────────┤
│ SSL             │ No (HTTP)            │ Sí (HTTPS)           │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Environment     │ .env.local           │ Environment Variables│
│ Variables       │                      │ (en plataforma)      │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Performance     │ Desarrollo (Slower)  │ Optimizado (Faster)  │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Uptime          │ Mientras ejecutas    │ 99.9% (SLA)          │
│                 │ el servidor local    │                      │
├──────────────────┼──────────────────────┼──────────────────────┤
│ CDN             │ No                   │ Sí (Vercel Global)   │
├──────────────────┼──────────────────────┼──────────────────────┤
│ Backups DB      │ Manual               │ Automáticos (Render) │
└──────────────────┴──────────────────────┴──────────────────────┘
```

---

## Notas y Consideraciones

### Performance
- Frontend: ~112 KB gzipped (Vercel CDN)
- Backend: ~50 MB (Node.js + dependencies)
- Database: ~5 MB (tipícamente)
- Latency: ~100-300ms promedio (red + processing)

### Escalabilidad
- Frontend: Automáticamente escalable (CDN global Vercel)
- Backend: Manual (Render: upgrade plan)
- Database: Manual (Render: upgrade instance)

### Costos
- Frontend (Vercel): Free tier
- Backend (Render): $7/month hobby tier
- Database (Render): Included con backend
- Total: ~$7/month

### Monitoreo
- Vercel Dashboard: Deployments, analytics, logs
- Render Dashboard: Logs, metrics, health
- GitHub Actions: Workflow status

---

**Documento generado**: Diciembre 10, 2025  
**Versión**: 1.0  
**Repositorio**: https://github.com/BrandonQuinte/Proyecto_Final
